// = assignment operator
// == comparison operator (compare if values are equal)
// === strict equality operator (compare if values & datatypes are equal) (IMP)
// != inequality operator
// !== strict inequality operator (IMP)

const pi = 3.14; // integer number

if(pi === "3.14") { // string number
    console.log("That is pi!");
}
else{
    console.log("That is NOT pi!");
}